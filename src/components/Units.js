import React from "react";
import '../styles/Units.css';

export default function Units(){
    return(
        <section className="unitsContainer p-4">
            <section>
                <h1 className="fs-4">Unidades</h1>

            </section>
        </section>
    );   
}